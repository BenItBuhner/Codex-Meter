<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <application
        android:usesCleartextTraffic="true"
        tools:replace="android:usesCleartextTraffic">
        <activity
            android:name="dev.bennett.codexmeter.UsagePaceDemoActivity"
            android:exported="true"
            android:theme="@style/AppTheme" />
        <!-- Debug-only: allow adb to open About for visual verification. -->
        <activity
            android:name="dev.bennett.codexmeter.AboutActivity"
            android:exported="true"
            tools:replace="android:exported" />
        <activity
            android:name="dev.bennett.codexmeter.AboutDependenciesPreviewActivity"
            android:exported="true"
            android:theme="@style/AppTheme" />
    </application>
</manifest>
